# This script calculates the area of a rectangle given its length and width.

# Define the variables for the length and width of the rectangle
length = 10  # Length of the rectangle
width = 5   # Width of the rectangle

# Calculate the area of the rectangle using the formula: Area = length * width
area = length * width

# Print the calculated area in the specified format
print(f"The area of the rectangle is: {area}")

# To execute this script:
# 1. Save the code above into a file named 'rectangle_area.py'.
# 2. Open your terminal or command prompt.
# 3. Navigate to the directory where you saved the file.
# 4. Run the script using the command: python3 rectangle_area.py

